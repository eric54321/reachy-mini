# apps/

Each subfolder here is a self-contained Reachy Mini app (CLI-generated,
Hugging Face Spaces-compatible structure). One folder per app, each with
its own requirements/config, so any single app can be split into its own
repo later without untangling shared state.
